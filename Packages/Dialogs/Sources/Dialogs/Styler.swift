import DesignSystem
import SwiftUI

struct Styler {
    let backgroundColor: Color = .white
    let contentSpacing: CGFloat = 16
    let font: Font = .pixelBody
    let titleFont: Font = .pixelTitle
    let foregrondColor: Color = .black
    let fontSize: CGFloat = Font.pixelBodySize
    let lineSpacing: CGFloat = 12
    let displayLines: Int = 10
}
