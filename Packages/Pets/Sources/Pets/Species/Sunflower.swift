import Foundation
import DesktopKit

extension Pet {
    
    static let sunflower = Pet(
        id: "sunflower",
        capabilities: .defaultsStatic,
        fps: 0.5,
        movementPath: .front,
        dragPath: .front,
        speed: 0
    )
}
